# JobHunterGPT Build Instructions

Inspect all files in this repository. Run the unit tests before editing. Improve validation, documentation, and test coverage without changing verified candidate facts. Keep credentials outside source control. Run the tests again after editing and report the files changed plus the exact test result.